# Python 3.10 (the notebook was executed on 3.10)
pandas
numpy
matplotlib
seaborn
xgboost
tensorflow

# Pinned below 1.6 on purpose. The notebook calls
#   mean_squared_error(..., squared=False)
# which scikit-learn deprecated in 1.4 and REMOVED in 1.6. On 1.6 or newer the
# last two model cells raise TypeError. Either keep this pin, or switch those
# calls to root_mean_squared_error().
scikit-learn<1.6
